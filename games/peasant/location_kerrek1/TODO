TODO:
	+ handle properly loading regular vs robe sprites at load time
	+ proper sound when lifting up belt
	+ In theory can get belt when muddy, do we care?
	+ (done) only decompress the sprites that are needed

Animation issues:
	+ technically in smash the peasant head is squished for a frame
		when going into the ground, it would be easy to add it I just
		forgot at the time
	+ There's an extra kerrek falling frame that's a slight rotation
		on an existing one that we don't implement
	+ When a message is displayed, the kerrek stops
		coming toward you but should still animates in place
		(as per the real game)
		(such as a "look kerrek")

	+ (done) handle being smashed at extreme edge of screen
	+ (done) allow kerrek to hit from left (instead of just right)

Sound
	+ kerrek arrival sound still isn't the best
	+ kerrek falling (falling)
	+ raise_up when pick up belt

Parser:
	+ double check we handle all the various combinatios
		"shoot kerrek" or if you can "shoot bow" too or
		just "shoot", etc


%%%%%%%%%%%%%%%%%%%%%%

Kerrek logic notes:

+ All maps: if kerrekdead + havebelt then increment screens_since_belt_taken

+ Arrive to either place

	if !kerreckDead
	if random()<0.8 then kerrek there
		kerrekPresent=true
		play sound
		walkSpeed=2;
	if dead, check screensincebelttaken
		<10 dead.  draw body, with belt if not taken yet
		10..15 "rot" with animation
		15+ bones
	
updateKerrek
	if kerrek_x>player_x then kerrek_dir=left,x-=walk_speed
	else kerrek_dir=right,x+=walk_speed

	if kerrek_y+kerrek_height>player_y+player_height move up
	else move down (by walkspeed)

	if abs(kerrek_y+kerrek_height - player_y+player_height)
		+ abs(kerrek_x-player_x)  < 50 then start collision?

	start_collision
		stop input
		if above equation <40 then start attack
			make player invisible
			start animation
			show text
			gameover

		else
			keep walking toward you with speed being +/-2




